<%#= we switch the @ answer below to be just answer %>
<%= answer.errors.full_messages.join(", ") %>
<%#= form_for @answer, url: question_answers_path(@question) do |f| %>
<%# adding a remote: true option to the form makes it so that the form submits
    with AJAX (prevent the default as well) %>
<%= simple_form_for [@question, answer], remote: true do |f|  %>
<%#= form_for [@question, @answer] do |form_helper|  %>
  <%= f.input :body, label: false %>
  <%= f.submit data: {disable_with: "submitting..."}, class: "btn btn-primary" %>
<% end %>
